// components/home/StateSelector.js
// Server Component — renders the state selection UI on the home page
// Receives: available_states array and coming_soon array from states.json
// Displays clickable cards for CA, TX; greyed-out cards for coming_soon states

// TODO: Accept props: { availableStates, comingSoon }
// TODO: Render clickable state cards that link to /state/{stateCode}
// TODO: Render coming_soon states as greyed-out / disabled cards
// TODO: Style per mockup design

export default function StateSelector({ availableStates, comingSoon }) {
  // TODO: implement
  return(
    <button className='rectangle mx-auto' >Clickable map placeholder</button>
  )
}
