// components/district/DistrictHeader.js
// Server Component — renders district metadata, rep info, demographics
// Receives: district overview.json data as props
//
// Shows representative name, party, photo, assumed office date
// Shows district demographics (population, median income, urban %, Cook PVI)
// Shows CES sample info (n respondents, confidence note)

// TODO: Accept props: { data }
// TODO: Render representative photo, name, party badge
// TODO: Render demographics grid
// TODO: Render CES sample size and confidence note
// TODO: If data is null, render DataUnavailable fallback

export default function DistrictHeader({ data }) {
  // TODO: implement
}
