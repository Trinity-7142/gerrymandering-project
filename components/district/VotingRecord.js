// components/district/VotingRecord.js
// Server Component — renders representative's voting record by issue
// Receives: representative.json data as props
//
// Shows votes grouped by issue bucket
// Each vote: bill number, title, date, yea/nay, direction (pro/anti regulation), source link
// Per-issue summary: total votes, pro vs anti count, absences
// Overall stats: total tracked votes, attendance rate

// TODO: Accept props: { data }
// TODO: Render votes grouped by issue_id (expandable sections)
// TODO: For each vote: bill, title, date, vote badge (yea/nay), direction, source link
// TODO: For each issue: summary stats
// TODO: Render overall voting stats (total tracked, attendance rate)
// TODO: If data is null, render DataUnavailable fallback

export default function VotingRecord({ data }) {
  // TODO: implement
}
