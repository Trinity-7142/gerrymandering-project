// components/state/StateHeader.js
// Server Component — renders state name, delegation composition, external links
// Receives: overview.json data as props
// Includes PlanScore outbound link from external_links.planscore field

// TODO: Accept props: { data } where data is the full overview.json object
// TODO: Render state name, total districts, delegation composition (D/R count)
// TODO: Render external links (PlanScore) if present in data.external_links
// TODO: Handle missing external_links gracefully (some states may not have them)

export default function StateHeader({ data }) {
  // TODO: implement
}
