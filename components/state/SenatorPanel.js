// components/state/SenatorPanel.js
// Server Component — renders side-by-side senator comparison
// Receives: senators.json data as props
// Task F6 — similar to AlignmentScore but for two senators
//
// Shows alignment scores, issue-by-issue breakdown, and key votes for both senators
// Consider tabbed or split layout for comparison

// TODO: Accept props: { data }
// TODO: Render both senators side-by-side
// TODO: For each senator: name, party, photo, overall alignment score + label
// TODO: For each senator: issue-level alignment breakdown with salience weights
// TODO: For each senator: key votes by issue (expandable/collapsible)
// TODO: Show CES state sample info (shared between both senators)
// TODO: Render interpretation scale
// TODO: If data is null, render DataUnavailable fallback

export default function SenatorPanel({ data }) {
  // TODO: implement
}
