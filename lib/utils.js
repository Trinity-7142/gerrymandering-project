// lib/utils.js
// Formatting and helper utilities

// TODO: formatPercent(decimal) — converts 0.72 to "72%"
// TODO: formatScore(score) — converts 0.74 to "74" or "0.74" depending on display context
// TODO: getAlignmentLabel(score) — returns "Low/Partial/Moderate/Strong Alignment"
// TODO: getAlignmentColor(score) — returns color based on score thresholds
// TODO: getGradeColor(grade) — returns color for Princeton grade (A/B/C/D/F)
// TODO: parseDistrictId(id) — splits "CA-11" into { state: "CA", number: 11 }
// TODO: formatDistrictId(state, num) — "CA" + 1 → "CA-01" (zero-padded)
